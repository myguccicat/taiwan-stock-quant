# 開發日誌：AI 供應鏈股票池自動化升級

**日期**：2026-07-07
**階段**：股票池擴展（方向一完成）

---

## 背景

原本的量化系統使用 48 檔手動挑選的自選股作為模型候選池，存在以下限制：

- 需要人工維護，耗時費力
- 股票池品質決定策略報酬上限——模型再強，也只能從這 48 檔裡挑
- 高度集中於 AI 供應鏈主題，缺乏系統性的候選股篩選機制

**目標**：用客觀條件自動篩選全市場電子股，擴大候選池，每週自動更新。

---

## 新增檔案：`update_stock_pool.py`

### 功能說明

每週日執行一次，自動完成以下流程：

```
從 TWSE 抓上市股票清單（1089 檔）
        +
從 TPEX 抓上櫃股票清單（889 檔）
        ↓
產業篩選（保留電子相關 8 大產業）
        ↓
用 yfinance 下載近 30 日股價資料
        ↓
套用篩選條件（股價、成交量）
        ↓
自動更新 watchlist.py 的 MY_WATCHLIST
```

### 篩選條件（最終版本）

| 條件 | 數值 | 說明 |
|------|------|------|
| 最低股價 | 15 元 | 排除雞蛋水餃股 |
| 最高股價 | 3000 元 | 避免零股資金不足 |
| 最低成交量 | 5,000,000 股/日 | 確保流動性（約 5000 張） |

### 篩選產業代碼（TWSE 分類）

| 代碼 | 產業 |
|------|------|
| 23 | 半導體業 |
| 24 | 電子零組件業 |
| 25 | 電腦及周邊設備業 |
| 26 | 光電業 |
| 27 | 通信網路業 |
| 28 | 電子通路業 |
| 29 | 資訊服務業 |
| 30 | 其他電子業 |

---

## 調試過程紀錄

### 問題一：產業欄位格式

TWSE API 回傳的產業別是**數字代碼**（`"23"`、`"24"`），
不是中文文字，導致初版篩選完全沒有過濾到上市股票。

**修正**：`TARGET_INDUSTRIES` 改用數字代碼清單。

### 問題二：前 10 檔全是食品股

上市清單按代碼排序，代碼 12xx 是食品業，測試用 `head(10)` 時
全部都不是電子股，誤以為篩選失敗。

**修正**：確認邏輯正確後直接跑全量。

### 問題三：成交量單位混淆

yfinance 回傳的成交量單位是「**股**」，台股 1 張 = 1000 股。
初版除以 1000 換算成張，導致門檻設定混亂。

**最終方案**：統一用「股」為單位，門檻設為 5,000,000 股（≈5000 張）。

### 問題四：舊資料保留邏輯覆蓋新篩選

`update_watchlist_py` 函式原本有「保留舊的自選股」邏輯，
導致不管新篩選出幾檔，結果都是上次的 351 檔。

**修正**：移除保留舊資料的邏輯，每次完全以新篩選結果取代。

### 問題五：世芯-KY、奇鋐不在清單

這兩檔雖然在你的自選股裡，但成交量確實低於門檻：
- 世芯-KY（3661）：約 175 萬股/日（≈1,759 張）
- 奇鋐（3017）：約 393 萬股/日（≈3,934 張）

**決策**：確認為真實流動性不足，直接淘汰，不手動保留。

---

## 最終結果

| 項目 | 數值 |
|------|------|
| MY_HOLDINGS（永豐金庫存） | 14 檔 |
| MY_WATCHLIST（自動篩選） | 140 檔 |
| ALL_STOCKS（模型候選池） | **154 檔** |
| 原本候選池 | 48 檔 |
| 擴展倍數 | **3.2 倍** |

---

## 與原本 48 檔的差異

**保留的重要股票**（確認在新池子裡）：
- 台積電、聯發科、凱美、訊芯-KY、群創、國巨 ✓

**淘汰的股票**（流動性不足）：
- 世芯-KY、奇鋐（成交量低於 5000 張門檻）

**新增的股票**（原本沒有但現在加入）：
- 聯電、友達、力晶積成、南茂、仁寶等 100+ 檔高流動性電子股

---

## 執行方式

```bash
# 每週日執行一次
python update_stock_pool.py
```

執行時間約 **15-20 分鐘**（需要逐一下載 1000+ 檔股票資料做篩選）。

---

## 下一步（待完成）

- [ ] Step 2：產業分類特徵——把 154 檔分群，加入群組動能特徵
- [ ] Step 3：外資動向整合——三大法人資金流向加進模型
- [ ] 回測對比：新版（154檔）vs 舊版（48檔）Spearman IC 比較
