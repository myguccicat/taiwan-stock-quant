# 台股量化選股專案 — 專案紀錄

最後更新：2026-08-30（新快取基準 + 視窗1/6診斷結果）

---

## 1. 專案總覽

目標：從 AI 供應鏈相關的台股（上市＋上櫃）中，用橫斷面排名模型每天挑出前5名股票做多，
搭配產業輪動特徵，目前策略設計為**純做多**（不放空後5名）。所有腳本都放在
`C:\Users\user\Desktop\stock`。

核心資料來源：`watchlist.py`（`MY_HOLDINGS` 目前持股 + `MY_WATCHLIST` 每週自動篩選 +
`ALL_STOCKS` 兩者合併），由 `update_stock_pool.py` 每週日自動更新。

另外資料夾裡有 `day27_real_trading_log_*.md` 系列，顯示這套系統已經連結到實際下單
（透過永豐/SinoPac API），不只是回測研究——這點會影響之後怎麼看待驗證結果的嚴謹度，
值得留意。

---

## 2. 檔案總覽

依實際資料夾分類整理（`C:\Users\user\Desktop\stock`）：

### 2.1 核心程式
| 檔案 | 功能 |
|---|---|
| `main.py` | ❓尚未看過內容——可能是排程入口或主執行檔，需要你提供內容才能寫進紀錄 |
| `watchlist.py` | 股票池資料（持股 + 觀察名單），`update_stock_pool.py` 自動維護 |
| `update_stock_pool.py` | 每週篩選 AI 供應鏈股票、更新 `watchlist.py` |
| `daily_run.py` | 每日排程，含 `sync_holdings_from_sinopac`（從券商同步庫存） |

### 2.2 模型與研究腳本
| 檔案 | 功能 |
|---|---|
| `day28_sector_ranking.py` | 核心模型：橫斷面排名 + 產業特徵（RandomForest），純做多策略主程式，目前採用版本 |
| `day29_sector_foreign_flow.py` | 在day28基礎上加外資流向特徵（`sector_foreign_flow`、`foreign_vs_sector`），2026-08-11實驗結論：**不採用**——加入後 IC 從+0.0205降到+0.0151，外資動向已被市場反映在股價中，詳見3.6節 |
| `day30_walkforward.py` | Walk-forward 滾動驗證，把歷史切成多個訓練/測試視窗 |
| `day31_naive_vs_model.py` | 對照組：完整 ML 模型 vs 只用 `sector_momentum` 排名的簡單規則 |
| `day32_window5_diagnostic.py` | 深挖指定視窗，比較 ML vs 簡單版規則實際選了哪些股票 |
| `day33_offset_sensitivity.py` | 檢驗單一視窗的複利報酬對「從哪天開始抽樣」的敏感度 |

### 2.3 交易與開發日誌
| 檔案 | 內容 |
|---|---|
| `day27_real_trading_log_0707.md` | ❓實戰交易紀錄（7/7），尚未看過完整內容 |
| `day27_real_trading_log_0716.md` | 實戰交易紀錄（7/16）：換倉執行面板，記錄「103檔雙軌制精準股票池」策略下的實際買賣（例如全數出清8046南電、2375凱美，新兵進場1568倉佑、6451訊芯-KY等），含委託股數、成交價、預估增減金額 |
| `dev_log_0707.md` / `dev_log_0712.md` | ❓開發日誌，尚未看過內容 |
| `dev_log_0811.md` | 「三步驟升級」開發日誌：Step1股票池擴展(48→103檔，2026-07-12)、Step2產業特徵(2026-07-16，即day28)、Step3外資流向(2026-08-11，即day29，結論不採用)，詳見3.6節 |
| `operation_manual.md` | ❓操作手冊，尚未看過內容 |

### 2.4 資料與追蹤檔案
| 檔案 | 內容 |
|---|---|
| `market_data_cache.pkl` | 共用資料快取（day28/30/31/32/33 都讀同一份，見第6節） |
| `ic_tracking.csv` | day28 每次執行自動記錄的 IC 追蹤表 |
| `filter_failed_2026-08-25.csv` | `update_stock_pool.py` 篩選失敗明細（分類原因，見3.x節） |
| `day31_walkforward_comparison.csv` | day31 walk-forward 各視窗 ML vs 簡單版對照結果 |

### 2.5 圖表輸出
`day28_sector_ranking.png`、`day29_sector_foreign_flow.png`、`day30_walkforward.png`、
`day31_naive_vs_model.png`、`ML_BACKTEST_1.jfif` ——對應腳本執行後的診斷圖

---

## 3. 修復過的問題（依時間順序）

### 3.1 `update_stock_pool.py`：ETF 誤入股票池
`00679B`（元大美債20年）、`00927`（群益半導體收益）是 ETF，價格行為跟股票不同，
會讓技術指標失真。已加入 `BLACKLIST`，`daily_run.py` 的 `sync_holdings_from_sinopac`
也加了 `ETF_BLACKLIST` 在庫存同步時排除。

### 3.2 `update_stock_pool.py`：`TWSE_TARGET` 產業代碼標錯
原本 `{"23","24","25","30"}` 的註解寫「半導體業、電子零組件業、電腦及周邊設備業、
其他電子業」，但實際 TWSE 代碼對照是：
`23=油電燃氣業　24=半導體業　25=電腦及週邊設備業　28=電子零組件業　30=資訊服務業　31=其他電子業`。
證據：黑名單裡的「台灣汽電共生」(9911) 正是油電燃氣業，證實代碼 23 混進了不相關產業；
華通(2313) 的產業別是 28，原本因此被排除。已修正為 `{"24","25","28","31"}`，
華通等電子零組件業股票已正確納入。

### 3.3 `day28_sector_ranking.py`：累積報酬複利重疊 bug
`future_3d`（未來3日報酬）逐日計算，但相鄰交易日的3日報酬區間重疊了2/3，
`cumprod()` 直接複利會把同一段行情算了將近3次，導致 IC 幾乎是雜訊
（+0.0029）卻能算出 +1000%+ 的累積報酬。修正：新增 `nonoverlap_mask()`，
複利只用每 `FORWARD_DAYS`（3）天一個樣本的不重疊區間；IC 維持逐日全樣本計算
（不受複利邏輯影響）。

### 3.4 `day28_sector_ranking.py`：空清單導致 crash
`evaluate()` 若整個視窗每天候選股都不足 `TOP_N*2`，原本回傳空 DataFrame 會在
`.set_index("date")` 時直接報錯。已加防呆，回傳結構正確的空表。

### 3.5 策略設計：改為純做多
驗證發現「放空後5名」歷史上常跟大盤同步甚至只小輸，不建議實際放空。
決策面板、報表用語、圖表全面調整為「純做多前5名」為主要策略指標，
放空/多空組合數字降級為「僅供研究對照」。

### 3.6 模型演進歷史：三步驟升級（源自 `dev_log_0811.md`）
在本對話開始前，模型已經歷過一輪系統性升級，記錄在案：

| 版本 | 檔案 | 完成日期 | Spearman IC | 最重要特徵 | 採用？ |
|---|---|---|---|---|---|
| Step0 純技術面 | `day27_ranking_3day.py` | — | +0.0206 | r1（22%） | 基準版本 |
| Step1 股票池擴展 | （48→103檔） | 2026-07-12 | — | — | 世芯-KY、奇鋐因成交量不足被淘汰；太陽能/LED明確排除 |
| Step2 +產業特徵 | `day28_sector_ranking.py` | 2026-07-16 | +0.0205 | sector_momentum（48%） | **✅ 主力，目前採用** |
| Step3 +外資流向 | `day29_sector_foreign_flow.py` | 2026-08-11 | +0.0151 | sector_momentum（47%） | ❌ IC下降，不採用（與Day26個股籌碼實驗結論一致） |

**⚠️ 需要注意的落差**：`dev_log_0811.md` 記錄 day28 在2026-07-16當時的 IC 是
**+0.0205**，但本對話裡（2026-08月）實際重跑 day28 得到的 IC 持續下滑——最早
+0.0025～0.0029，2026-08-30用新快取基準重跑後只剩 **+0.0006**，幾乎完全歸零。
`dev_log_0811.md` 自己也寫下了警示：

> 「IC 近期趨勢警示：day29 的 Rank IC 圖顯示，2026-07 之後 20日移動平均已接近零線⋯⋯
> 建議持續監控，若 IC 持續低於 +0.02 超過一個月，需重新評估模型架構。」

**這個門檻已經被踩過了，而且持續惡化中**——現在的 IC 不只低於 +0.02，是幾乎等於零，
而且已經不只一個月。照 `dev_log_0811.md` 自己訂的標準，**模型架構重新評估已經是逾期事項**，
不是「之後再說」的優先度，應該排進第7節待辦的前面。

---

## 4. 已驗證的發現

> 4.1～4.6 是資料快取機制建立**之前**的發現（用舊資料跑出來的），保留是因為
> 方法論與診斷邏輯本身仍然成立；4.7 之後是 2026-08-30 建立新快取基準
> 後的最新結果，數字以 4.7 之後為準。

### 4.1 單一測試期（舊資料，2026-01-29 ～ 2026-08-20，134個交易日）
- 純做多前5名累積報酬 +255～256%，同期全市場基準 +60.6%，超額 +194～195%
- Spearman Rank IC 只有 +0.0025～0.0029，接近雜訊等級
- 前5名入選頻率最高的股票集中度不算高（單檔最高約19%），報酬不是靠一兩檔飆股撐出來的

### 4.2 Walk-forward 滾動驗證（舊資料，6個視窗，訓練窗擴張式，測試窗60天）
- 83%（5/6）視窗超額報酬為正，平均超額 +21～23%
- 平均 IC 只有 +0.0013～0.0014，一樣接近雜訊
- 超額報酬**集中在特定視窗**（尤其視窗5），不是每個視窗均勻分布：
  - 視窗1、2、3、6：簡單版規則表現持平甚至更好
  - 視窗4：ML小贏
  - 視窗5（2026-02-09～2026-05-18）：ML大幅超前（超額+83.8% vs 簡單版-13.7%）

### 4.3 特徵重要性
`sector_momentum` 長期佔 37～43%，是最重要特徵；`r1` 次之（約16～22%）；
`sector_rank` + `inter_sector_rank`（真正做「產業內選股」的兩個特徵）合計常不到2%。
一度懷疑模型只是在追蹤產業動能，沒有真正的個股選擇能力。

### 4.4 ML模型 vs 簡單版sector_momentum規則對照（舊資料）
- **單一測試期**：簡單版規則表現遠不如ML，甚至輸給大盤基準（超額-30.4%，捕捉比例-16%）
- **Walk-forward（跨6視窗）**：簡單版捕捉了ML約68%的超額報酬
- 這兩個數字差異很大，起初懷疑是方法論問題，後來確認主因是「單一測試期」跟
  「walk-forward視窗5」時間範圍高度重疊——ML的優勢集中在視窗5，放大了單一測試期的對比

### 4.5 視窗5選股差異診斷（day32，舊資料）
- 平均每天重疊檔數只有0.6/5，ML跟簡單版選股邏輯明顯分岔
- ML獨有選股（naive完全沒選到）平均未來3日報酬+4.24%，明顯高於naive獨有選股+2.04%
- ML獨有選股分散在11個產業，最集中的產業只佔26%，不是押注單一熱門產業
- 具體貢獻者：訊芯-KY(+10.96%)、禾伸堂(+18.54%)、IET-KY(+6.89%)、新唐科技(+5.96%)、
  聯茂電子(+6.46%)，橫跨晶片設計、製造封測、其他、PCB載板等不同產業
- **結論**：這個視窗裡ML確實展現出「產業內選股」能力，不只是換了個熱門產業

### 4.6 視窗2選股差異診斷 + 複利抽樣敏感度分析（舊資料，發現了資料不一致問題）
- day32對視窗2的診斷顯示ML獨有選股(+1.20%)其實略贏naive獨有選股(+0.82%)，
  跟day30/31報告的「視窗2 ML超額-22.3%」表面矛盾
- **重要發現：這不是矛盾，是資料不一致**——day32/day33重跑時，因為沒有資料快取，
  `yfinance` 重新下載了「今天往回3年」的資料，加上 `watchlist.py` 股票池本身也變了
  （104檔→101檔），導致「視窗2」的實際測試期日期悄悄從
  `2025-05-22~2025-08-14` 偏移到 `2025-05-23~2025-08-18`，已經不是同一批資料
- 這個發現直接促成第6節的資料快取機制

### 4.7 新快取基準（2026-08-30建立）：IC持續探底
`day28_sector_ranking.py --refresh-cache` 重新下載並建立新快取（101檔股票、730個
交易日），成為之後所有分析的統一基準：
- Spearman Rank IC：**+0.0006**，比之前任何一次都更接近零
- 純做多前5名累積報酬 +246.5%，全市場基準 +73.3%，超額 +173.2%——報酬依然亮眼，
  跟IC幾乎為零的矛盾持續存在
- IC走勢圖（20日均）：3～6月維持 +0.05～+0.10尚屬健康，**7月後明顯下滑，8月初一度
  探到-0.15～-0.2**，最近才勉強拉回接近零——走勢跟`dev_log_0811.md`的警示完全吻合，
  且惡化仍在持續

### 4.8 新快取基準下的Walk-forward（day30重跑）
6個視窗（測試期涵蓋2025-02-24～2026-08-13），整體數字比舊資料更誇張：
- 平均IC：+0.0002；平均超額報酬：+32.0%；勝率83%（5/6）
- 串接後累積報酬 +733.4%（前5名做多）vs 基準+141.7%——比舊資料的+399～430%更高，
  原因是這次視窗3、4、5**連續三個**都大贏（超額+47.5%、+62.8%、+68.4%），複利疊加放大

**逐視窗細節，拼出一個「條件式優勢」的圖像**：

| 視窗 | 測試期 | IC | 超額報酬 | 備註 |
|---|---|---|---|---|
| 1 | 2025-02-24~2025-05-22 | +0.0233（正） | **-1.5%**（負） | 訓練資料量最少（僅300天下限）；offset敏感度分析確認**非雜訊**，3個offset全部naive勝出（見下） |
| 2 | 2025-05-23~2025-08-18 | -0.0234 | +10.0% | 優勢薄弱 |
| 3 | 2025-08-19~2025-11-14 | +0.0119 | +47.5% | 明顯占優 |
| 4 | 2025-11-17~2026-02-10 | +0.0114 | +62.8% | 明顯占優 |
| 5 | 2026-02-11~2026-05-20 | +0.0355 | +68.4% | 明顯占優，訓練資料量最多 |
| 6 | 2026-05-21~2026-08-13 | **-0.0576**（史上最負） | +4.8% | 優勢仍在但明顯轉弱，見4.9節 |

**關鍵發現：單一視窗的IC正負，沒辦法可靠預測那個視窗的超額報酬好壞**（視窗1
IC正但報酬負，視窗6 IC最負但報酬還是正的）——這點跟day33在舊資料上發現的
「複利抽樣路徑雜訊」現象一致，但視窗1這次額外做了offset敏感度驗證，
確認**不是雜訊、是真實的表現差異**（見4.9節）。

### 4.9 視窗6選股診斷 + 視窗1敏感度分析（day32/day33，新快取基準）

**視窗6（day32診斷）**：ML優勢還在但明顯稀釋、雜訊化：
- 平均每天重疊檔數0.3/5，ML獨有選股平均報酬+1.13% vs naive獨有+0.68%，
  差距只有+0.45%（約為視窗5那次+2.20%的五分之一）
- ML獨有的前10名裡贏家（禾伸堂+10.95%、景碩+8.12%、IET-KY+7.42%）跟輸家
  （大毅科技-5.85%、富鼎先進-4.49%）混在一起，不像視窗5那樣乾淨一致
- 這個視窗訓練出的模型，`sector_momentum`重要性從常態38～43%掉到**27.79%**，
  `vol_5d`、`vol_ratio`等原本排名後段的技術指標權重明顯提升——模型有在自適應調整，
  但調整後IC還是負的，代表沒能完全補回流失的預測力

**視窗1（day33敏感度分析）**：3個offset（0/1/2）全部一致，ML超額報酬平均只有+1.4%
（範圍-1.5%~+3.9%），naive規則在**全部3個offset都贏過ML**（平均+6.6%，範圍
+3.3%~+12.2%）。跨度不大，判定為真實表現差異，不是抽樣雜訊。
推測原因：視窗1訓練資料量卡在`min_train_days`下限（300天），可能資料量不夠，
模型還沒學出可靠判斷——**這個推測尚未驗證**，見第7節。

**綜合結論**：ML相對簡單規則的優勢**不是無條件成立**，目前證據顯示比較像是
「訓練資料量夠多 + 特定市況（視窗3~5那段）」的組合才會顯著超前；訓練資料量少
（視窗1）或近期訊號本身轉弱（視窗6，對應7~8月IC下滑）時，優勢會消失或大幅萎縮。

---

## 5. 目前策略設計摘要

- **純做多前5名**，不放空後5名（後5名只作為「避開名單」參考，不建議實際放空）
- 訓練特徵：11個技術指標（r1, r5, r20, ma5_ratio, ma20_ratio, ma60_ratio, vol_ratio,
  vol_5d, rsi14, bb_pos, near_high）+ 3個產業特徵（sector_rank, sector_momentum,
  inter_sector_rank）
- 模型：RandomForestRegressor（n_estimators=300, max_depth=6, min_samples_leaf=20,
  random_state=42）
- 換倉週期：3個交易日（`FORWARD_DAYS`）

---

## 6. 資料快取機制（2026-08-25新增，2026-08-30已建立第一份正式基準）

### 問題
每支腳本各自呼叫 `yfinance` 下載「今天往回3年」的資料，導致不同時間點、不同腳本
執行時，同名的「視窗N」實際上可能對應到不同的日期範圍與股票池，數字無法互相比較
（見4.6節）。

### 解法
`day28_sector_ranking.py` 新增 `load_or_download_market_data()`：
- 有快取（`market_data_cache.pkl`）且不要求刷新時，直接讀快取
- 快取內容跟目前 `watchlist.py` 的股票清單不一致時，印警告但仍使用快取（不自動刷新）
- `day30/31/32/33` 全部改用這個函式，並支援 `--cache-path`、`--refresh-cache` 參數

### 目前基準
2026-08-30 用 `--refresh-cache` 建立了第一份正式基準（101檔股票、730個交易日），
之後 day30（4.8節）、day32/day33（4.9節）都已經在這份基準上重跑過，數字彼此可比較。
之後如果要再刷新基準，記得同一批新資料要把 day30～33 都重新跑一輪，才能維持
「同一批資料」的前提。

### 使用方式
```powershell
# 建立/更新快取（任何一支腳本加這個參數都可以）
python day28_sector_ranking.py --refresh-cache

# 之後所有腳本預設會讀同一份快取，彼此才可比較
python day30_walkforward.py
python day31_naive_vs_model.py
python day32_window5_diagnostic.py --window-index 5
python day33_offset_sensitivity.py --window-index 2
```

---

## 7. 尚待解決 / 下一步

- [ ] **⚠️ 模型架構重新評估（逾期，且持續惡化）**：`dev_log_0811.md` 自訂的標準
      「IC持續低於+0.02超過一個月需重新評估架構」已經被觸發，最新一次基準測得IC只有
      +0.0006，比之前更低，這件事的優先度應該高於下面其他項目
- [ ] **驗證「訓練資料量」假說**：4.9節推測視窗1表現差可能是訓練資料量卡在
      `min_train_days`下限（300天）導致，這個推測尚未驗證——可以嘗試把
      `--min-train-days`調高（例如400、500）重跑day30，看視窗1這種「資料量不足」
      的情況是否消失或改善
- [ ] **`dev_log_0811.md` 原本待處理清單，尚未確認目前狀態**：
      自動下單串接（原註記「待模型驗證穩定後啟動」，但`day27_real_trading_log_0716.md`
      顯示已有實際下單紀錄，需要確認現況）、README更新（反映三步驟實驗結論）、
      day28整合進`daily_run.py`取代原有排名邏輯
- [ ] **補完檔案紀錄**：`main.py`、`dev_log_0707.md`、`dev_log_0712.md`、
      `operation_manual.md`、`day27_real_trading_log_0707.md` 目前只知道檔名，
      需要你分享內容或摘要，才能把這幾個部分寫進紀錄
- [ ] **延長歷史資料，涵蓋非多頭市況**：目前3年資料整體仍偏多頭／產業輪動明顯，
      沒有真正驗證過模型在修正、盤整、系統性下跌時的表現
- [x] ~~建立新的快取基準~~：已於2026-08-30完成，day30/32/33都已在新基準上重跑
- [x] ~~視窗2 vs 視窗5 對照~~：新基準下已改查視窗1（資料量少）與視窗6（近期轉弱），
      兩者都確認是真實表現差異、非抽樣雜訊，見4.9節
- [ ] **68% vs -16% 捕捉比例的落差**（舊資料）：新基準下尚未重新用day31驗證這個
      對照，但4.8～4.9節的「條件式優勢」發現（資料量+市況雙重條件）可能已經是
      更完整的解釋，不一定需要單獨再查

---

## 8. 常用指令備忘

```powershell
# 主模型（純做多前5名，日常使用）
python day28_sector_ranking.py

# Walk-forward驗證（較慢，重複訓練6次左右）
python day30_walkforward.py

# 模型複雜度對照（ML vs 簡單版sector_momentum規則）
python day31_naive_vs_model.py

# 深挖指定視窗的選股差異（--window-index對齊day30印出的編號）
python day32_window5_diagnostic.py --window-index 5

# 複利抽樣路徑敏感度分析
python day33_offset_sensitivity.py --window-index 2

# 每週更新股票池
python update_stock_pool.py
```
